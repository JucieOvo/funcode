# Funcode 发布说明

作者：JucieOvo  
日期：2026-04-03

## 1. 发布摘要

本次发布的是 `Funcode` 的第一版 Python 运行时子集。

它面向的是“终端代理式编码工作流”的 Python 实现路线，重点不在外壳包装，而在以下四层能力：

1. `agent`
2. `subagent`
3. `swarm`
4. `agentteam`

如果熟悉这一领域，你会发现它明显借鉴了成熟终端代理产品里已经被验证过的工作流哲学。  
但本项目不直接绑定任何原品牌，也不宣称与任何现有商业产品存在官方关系。

## 2. 现在已经完成的部分

本次发布已经完成的核心能力有：

1. Python 包结构与命令入口。
2. 多代理运行时的基础状态管理。
3. 子代理上下文分叉与恢复链路。
4. 团队协作状态面，包括团队、任务、邮箱、快照。
5. 任务依赖门控、并发门控与阻塞态表达。
6. worktree、工具范围、fork 上下文继承。
7. transcript 与 replacement-state 的落盘、恢复与继续执行。

这意味着当前版本已经具备真实运行的核心运行时，而不再只是概念验证。

## 3. 当前已全面对齐的范围

如果只看运行时语义，当前已全面对齐的部分是：

### 3.1 Agent

已具备：

- 独立代理定义
- 多次 run 持久化
- 生命周期状态迁移
- 后台与恢复路径

### 3.2 Subagent

已具备：

- 父子 run 关系
- fork 上下文继承
- transcript 连续性
- replacement-state 连续性
- interrupt / resume 后继续推进

### 3.3 Swarm

已具备：

- 团队对象
- 任务对象
- 邮箱消息面
- 快照与状态对账

### 3.4 Agentteam

已具备：

- 依赖门控
- 并发门控
- blocked 状态
- 单独调用可观测性

也就是说，当前版本最扎实的完成面，不是 UI，而是多代理运行时本身。

## 4. 当前未完成的部分

当前还没有完成到“全功能终端产品”级别的部分主要包括：

1. 更完整的交互外壳与体验层。
2. 更广泛的外围命令矩阵。
3. 更多平台级适配与测试覆盖。
4. 更丰富的示例、教程与分场景文档。

因此，本次发布的正确定位应该是：

**一个已经具备核心代理运行时能力的 Python 底座，而不是全部外围能力都补齐的终端产品。**

## 5. 发布内容范围

本次发布建议保留：

1. `pyproject.toml`
2. `README.md`
3. `.gitignore`
4. `RELEASE_FUNCODE.md`
5. `src/funcode/`
6. `src/tests/`（如需要测试）

本次发布不建议带入：

1. 临时验证目录
2. 运行时缓存目录
3. 旧迁移文档
4. 构建产物
5. 无关调试输出

## 6. 风险边界说明

本项目的介绍文案有意保持克制，原因有三点：

1. 只陈述本项目已经真实实现的能力。
2. 只表达“借鉴过成熟工作流哲学”，不表达品牌归属。
3. 避免把“语义对齐”误解为“品牌替代”。

因此，本次发布文案采用的是：

**隐晦表达来源方向，明确表达实现边界。**

## 7. 一句话定位

`Funcode` 当前最准确的定位是：

**一套面向终端代理式编码工作流的 Python 运行时实现，已经完成多代理核心语义，对外壳与外围能力仍保持持续建设。**
